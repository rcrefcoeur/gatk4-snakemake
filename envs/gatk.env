name: gatk-env
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - gatk4=4.6  # pin a concrete version if you need reproducibility; adjust as needed
  - picard
  - openjdk  # if needed; gatk packages often pull Java
  - samtools
