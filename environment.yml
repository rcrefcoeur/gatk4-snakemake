name: snakemake-run
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.11        # compatible with Snakemake 8.x
  - snakemake=8.11.2   # stable version with --use-conda support
  - mamba              # optional, speeds up solving
  - pip
  - git
